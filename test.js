// selecting using query selector using class names
// console.log(document.querySelector(`.message`).textContent);
// document.querySelector(`.message`).textContent = `correct number`;
// console.log(document.querySelector(`.message`).textContent);

// document.querySelector(`.number`).textContent = 13;
// document.querySelector(`.score`).textContent = 30;

// document.querySelector(`.guess`).value = `12`;
// console.log(document.querySelector(`.guess`).value);

//Handling click events

// document.querySelector(`.check`).addEventListener(`click`, function () {
//   console.log(document.querySelector(`.guess`).value);
//   document.querySelector(`.message`).textContent = `correct number`;
// });

// querry selector selects only the first element when there is multiple element with the same class name in that case we use querry selector all
// function() with () along click we directly be caledd incase of predefined functions domnot use  () then it will be executed after click event

// we can add remove class or directly change css in js eh abc.style.display=`block` or abc.classList.add(`hidden`);
